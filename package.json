{
  "name": "my-tth-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "npm --prefix client ci && npm --prefix client run build && npm --prefix server ci --include=dev && npm --prefix server run build",
     "repo:map": "node ./scripts/repo-map.mjs"
  },
  "engines": {
    "node": "20.x",
    "npm": "8.x"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "date-fns": "^4.1.0",
    "dotenv": "^17.2.1",
    "express": "^4.21.2",
    "react-datepicker": "^8.4.0"
  },
  "devDependencies": {
    "@types/axios": "^0.9.36",
    "@types/express": "^5.0.3"
  }
}
